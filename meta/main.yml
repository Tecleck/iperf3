---
galaxy_info:
  author: tecleck
  namespace: tecleck
  description: Installation of Iperf3 with option to run as server
  license: "license (BSD, MIT)"
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
  galaxy_tags:
    - iperf
    - iperf3
    - network
dependencies: []
# List your role dependencies here, one per line. Be sure to remove the '[]' above,
# if you add dependencies to this list.
